<h1>Hola <%=@user.name%>,
  <% if @user.gender %>
  	Bienvenido
  <%else%>
    Bienvenida
  <% end %>
</h1>
<hr>

<%= link_to 'Cerrar sesión', users_dashboard_logout_path%>


<h1>Properties currently for rent</h1>

<table>
  <thead>
    <tr>
      <th>Casa</th>
      <th>Dirección</th>
      <th>Lesee</th>
      <th>Precio/mes</th>
      <th>Disponible</th>
    </tr>
  </thead>

  <tbody>
    <% @propertylessor.each do |property| %>
      <tr>
        <td><%= property.name %></td>
        <td><%= property.address %></td>
        <td><%= property.lesee%></td>
        <td><%= property.price %></td>
        <td><%= property.available %></td>  
	<td><%= link_to "More info", properties_information_path(:id => property.id)%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h1>Properties you are paying for</h1>

<table>
  <thead>
    <tr>
      <th>Casa</th>
      <th>Dirección</th>
      <th>Precio/mes</th>
    </tr>
  </thead>

  <tbody>
    <% @propertylesee.each do |property| %>
      <tr>
        <td><%= property.name %></td>
        <td><%= property.address %></td>
        <td><%= property.price %></td>
	<td><%= link_to "More info", properties_information_path(:id => property.id)%></td>
      </tr>
    <% end %>
  </tbody>
</table>


